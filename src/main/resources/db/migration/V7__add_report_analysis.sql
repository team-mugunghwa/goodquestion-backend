-- ============================================================
-- 보호자 리포트 확장 (리포트 요건 2~7절)
-- 어휘·표현·논리 역량 분석, 대표 발화와 선정 이유, 가정 연계 대화 가이드를
-- 기존 summary/strengths/next_focus로는 담을 수 없다.
--
-- 컬럼을 넷으로 쪼개지 않고 jsonb 하나에 담는다. 요건 문서가 "일부 영역만
-- 구성해도 되고 화면 구성도 분리할 수 있다"고 열어 둔 상태라 구조가 더 바뀐다.
-- 쪼개 두면 그때마다 마이그레이션이 하나씩 쌓이는데, 리포트는 통째로 읽어
-- 화면에 뿌리는 데이터라 컬럼 단위로 조회하거나 조건에 쓸 일이 없다.
-- ============================================================

alter table reports
    add column analysis jsonb not null default '{}'::jsonb;