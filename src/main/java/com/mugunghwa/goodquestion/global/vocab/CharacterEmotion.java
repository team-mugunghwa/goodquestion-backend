package com.mugunghwa.goodquestion.global.vocab;

/**
 * 캐릭터 마음 변화 (표정·태도). 캐릭터 LLM이 대사와 함께 출력.
 * 값 목록은 프런트 표정 리소스와 함께 확정 필요 (현재 제안값).
 */
public enum CharacterEmotion { NEUTRAL, HAPPY, SAD, WORRIED, SURPRISED, RELIEVED }
